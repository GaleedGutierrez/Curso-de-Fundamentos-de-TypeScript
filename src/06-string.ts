(() => {
    let productTitle = 'My amazing product';
    // productTitle = 1;
    const productDescription = "It's my amazing product change";
    console.log({productTitle});
    const productPrice = 100;
    const isNew = true;
    const sumary = `
        title: ${productTitle}
        description: ${productDescription}
        price: ${productPrice}
        state: ${isNew}
    `;
    console.log({sumary});

    const myString: string = '';
})()
